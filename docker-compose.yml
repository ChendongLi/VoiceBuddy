services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: voicebuddy
      POSTGRES_USER: voicebuddy
      POSTGRES_PASSWORD: voicebuddy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U voicebuddy"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  pgdata:
