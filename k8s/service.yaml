apiVersion: v1
kind: Service
metadata:
  name: voicebuddy-service
  namespace: voicebuddy
  labels:
    app: voicebuddy
spec:
  type: ClusterIP
  selector:
    app: voicebuddy
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
